title: AppointmentResponse
type: object
required:
  - data
properties:
  data:
    $ref: './Appointment.yaml'
  included:
    type: array
    description: |
      The related resources named in the `include` query parameter.
      Omitted when nothing was included.
    items:
      oneOf:
        - $ref: './Patient.yaml'
        - $ref: './Doctor.yaml'
